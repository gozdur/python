# Tuples are like list but cannot change any value once you create a Tuple

coordinates = (4, 5)
print(coordinates)

coordinates = (4, 5)
print(coordinates[0])


# Difrence between tuples and lists
    # Tuples are not motifiable
    # Lists are made out of square brackets[] and tuples out of parenthasis ()

coordinates_list = [(6, 4), (6, 8, 9)] # this is a list that contains two tuples
print(coordinates_list)